# 为 JEV MLX 做贡献

[返回项目首页](README.md)

请使用独立环境和本地模型目录。不要安装到共享推理环境，不要修改模型权重，也不要为了提高基准成绩而停止其他应用。要求 Python 3.11 或更新版本。

```sh
python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install -e '.[dev]'
python scripts/check_docs.py
python -m pytest -m 'not model'
python -m ruff check src tests benchmarks scripts
python -m build
```

在 Apple Silicon 上，安装 `'.[mlx,dev]'` 以运行真实推理。一次只运行一个检查点，并为 `tests/test_model.py` 明确设置 `JEV_TEST_MODEL`。核心测试使用注入的后端；它们不能证明模型质量、Metal 执行或真实检查点的缓存正确性。

## 实现原则

- 先检查官方 MLX／MLX-LM 能力。在实现替代方案前，优先复用加载、量化、生成和缓存功能。
- 让模型负责语义。不要添加针对特定语句的正则表达式或隐藏的标准答案来改善示例或测试表现。
- 明确提供状态、动态候选、no-match 和 abstain。稳定的业务 ID 不得依赖内部 token 编码。
- 分开记录模型预测、宿主执行验证和回执。被拦截的错误动作，在评测中仍是模型错误。
- 为混合模型保留完整有效的缓存快照。中间内容变化会使其后所有依赖状态失效。
- 保持公共接口在没有 MLX 时仍可导入。正确性修改应增加有意义的回归覆盖，并执行与受影响行为相关的检查。
- 保留第三方材料的许可证和归属说明。

首版针对使用现有检查点的英文单步选择。扩展公共数据契约前，请先讨论更广泛的功能。文档应描述已观察的行为和明确限制，而非未来性能。

## 文档语言

每篇文档都保留完整的中文和英文版本，并登记在 `docs/locales.json`。只有两个首页互相链接；内页链接保持同一语言，并返回对应语言的首页。不要在内页添加语言选择器。翻译时保留原始提示词、命令和评测证据。运行 `python scripts/check_docs.py` 检查文档配对、本地链接和语言路径；CI 也会运行同一项检查。

## 评测修改

修改测试数据、提示词、阈值、指标、基线或缓存测试前，请阅读[评测协议](docs/evaluation.zh-CN.md)。开发数据和冻结测试数据彼此独立，并检查清单哈希。不要在冻结测试集上调参后，仍称其为留出测试。新调优的方法需要新的留出数据与来源记录。

所有示例与资源必须为虚构、专门新写并可公开使用。不得导出生产数据、对话、录音、截图或日志，包括声称已经匿名化的版本。基准产物中保留每次尝试与失败，并使用新的输出目录。不要只选择成功示例。

性能声明需要包含模型、量化、依赖、硬件、输入规模、内存与缓存细节。在相同状态／缓存条件下比较单编码与 JSON 基线。计时应同步 MLX。重复示例增加的是时间测量次数，不是独立质量样本数。原始分数和受限 softmax 在被称为正确概率前，需要单独的校准证据。

## 拉取请求与问题报告

描述问题、修改后的行为、验证与相关限制。提供复现命令，并标注未运行的检查。针对模型的问题，应包含公开检查点身份、依赖版本、一条小型虚构请求，以及完整结果或异常。分享产物前删除用户名、私有路径、密钥和无关日志。不要附带权重。

原创贡献采用 MIT 许可证。依赖和模型权重保留各自的许可证。发布流程见 [docs/releasing.zh-CN.md](docs/releasing.zh-CN.md)。
